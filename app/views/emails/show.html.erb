<%- model_class = Email -%>


<ul class="nav nav-tabs">
  <li role="presentation"><a href="<%= emails_path %>">Home</a></li>
  <li role="presentation" class="active"><a href="#"> <%= @email.reference_id.sub(".txt","") %>  </a></li>

      <!--<li><a href="#">Something else here</a></li>-->
      <!--<li role="separator" class="divider"></li>-->
      <!--<li><a href="#">Separated link</a></li>-->
      <!--<li role="separator" class="divider"></li>-->
      <!--<li><a href="#">One more separated link</a></li>-->

      <% if @email.attachments.count > 0 %>
      <li class="dropdown">
        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
          <%= image_tag "attachment.png", size: "20x20", alt: "attachment" %>
          Attachments
          <span class="caret"></span></a>
        <ul class="dropdown-menu">

          <% counter = 1 %>
          <% @email.attachments.each do |attachment| %>
              <li>
                <%= link_to attachment_path(attachment), :class => 'btn btn-default' do %>
                    <%= "Attachment-#{counter}" %>
                    <% counter = counter+1 %>
                <%- end -%>
              </li>
          <% end %>
        </ul>
      </li>
      <% end %>




  <div class="btn-group" role="group" aria-label="...">
    <div class="btn-group" role="group">
      <!--<button type="button" class="btn btn-default">Left</button>-->
      <% if current_user.voted_up_on? @email %>
        <%= button_to " Priviledged ", like_email_path(@email), :class => "btn btn-danger"%>
      <% else %>
          <%= button_to " Priviledged ", like_email_path(@email), :class => "btn btn-default"%>
      <% end %>
    </div>
    <div class="btn-group" role="group">

      <% if current_user.voted_down_on? @email %>
          <%= button_to " Not Priviledged ", dislike_email_path(@email), :class => "btn btn-success "%>
      <% else %>
          <%= button_to " Not Priviledged ", dislike_email_path(@email), :class => "btn btn-default "%>
      <% end %>


    </div>
  </div>

</ul>


<div class="container">
  <div class="row">
    <div >
        <div class="fieldset">
            <div class="email-text">
                <%=  readfile.html_safe %>
            </div>
        </div>
    </div>


        <div class='row-fluid'>
            <div class=" panel" id="graphContainer" data-series="<%= gethistory %>"
                 data-dt="<%= getdate %>" style="min-width: 310px; height: 300px; margin: 0 auto" >
            </div>
          <!--<div class="col-md-2 panel">-->
            <!--<table>-->
              <!--<% if @email.attachments.count > 0 %>-->
                  <!--<% counter = 1 %>-->
                  <!--<% @email.attachments.each do |attachment| %>-->
                      <!--<tr><td>-->
                        <!--<%= image_tag "attachment.png", size: "20x20", alt: "attachment" %>-->
                        <!--<%= link_to attachment_path(attachment), :class => 'btn btn-default' do %>-->
                            <!--<%= "Attachment-#{counter}" %>-->
                            <!--<% counter = counter+1 %>-->
                        <!--<%- end -%>-->
                      <!--</td>-->
                      <!--</tr>-->
                  <!--<% end %>-->
              <!--<% end %>-->
            <!--</table>-->
          <!--</div>-->
        </div>
  </div>
  <div text-align="center"> * The above graph plots Privilege propensity </div>
</div>









